/**
 * @file HeroSection.types.ts
 * @description Type definitions for the HeroSection domain component.
 * * @author serjimen
 * @date 2026-03-03
 * @version 1.0.0
 */

export interface HeroSectionProps {
  /** Optional extra CSS classes for the outer wrapper */
  className?: string;
  /** Optional ID for anchor navigation (defaults to 'hero') */
  id?: string;
}
