# ============================================
# Transcendence — Environment Variables
# ============================================
# Copy this file to .env:    cp .env.example .env
# ⚠️ NEVER commit .env — it's in .gitignore
#
# Ports default to 4200-range to avoid conflicts
# with other projects on the same machine.
# ============================================

# ─── Ports (host-side) ──────────────────────────────
# Change these if another project uses the same ports.
BACKEND_PORT=4200
FRONTEND_PORT=4201
PRISMA_STUDIO_PORT=4202
DB_PORT=4210
REDIS_PORT=4211
MAILPIT_UI_PORT=4212

# ─── Database (PostgreSQL 16) ───────────────────────
POSTGRES_USER=transcendence
POSTGRES_PASSWORD=transcendence
POSTGRES_DB=transcendence
# DATABASE_URL is constructed in docker-compose.dev.yml
# For local (non-Docker) dev, uncomment:
# DATABASE_URL=postgresql://transcendence:transcendence@localhost:4210/transcendence

# ─── Cache / Real-Time (Redis 7) ────────────────────
# REDIS_URL is set in docker-compose.dev.yml
# For local dev: REDIS_URL=redis://localhost:4211

# ─── Authentication ─────────────────────────────────
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-me-to-a-random-64-char-string
JWT_EXPIRATION=7d

# ─── OAuth 2.0 (42 API) ─────────────────────────────
# Register at: https://profile.intra.42.fr/oauth/applications
OAUTH_42_CLIENT_ID=
OAUTH_42_CLIENT_SECRET=
OAUTH_42_CALLBACK_URL=http://localhost:4200/api/auth/42/callback

# ─── Application ────────────────────────────────────
NODE_ENV=development
CORS_ORIGINS=http://localhost:4201

# ─── File Uploads ────────────────────────────────────
UPLOAD_MAX_SIZE_MB=10

# ─── API Rate Limiting ──────────────────────────────
API_RATE_LIMIT_TTL=60
API_RATE_LIMIT_MAX=100

# ─── Email (dev: Mailpit catches everything) ────────
# In production, replace with real SMTP:
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=true
# SMTP_USER=
# SMTP_PASS=
# EMAIL_FROM=noreply@transcendence.app
